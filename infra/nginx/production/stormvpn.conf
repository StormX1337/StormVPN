# Production virtual host with TLS (Let's Encrypt via certbot, see docs/DEPLOYMENT.md).
# Replace stormvpn.example with your domain and mount this file instead of conf.d/stormvpn.conf.

server {
  listen 80;
  listen [::]:80;
  server_name stormvpn.example www.stormvpn.example;
  location /.well-known/acme-challenge/ { root /var/www/certbot; }
  location / { return 301 https://stormvpn.example$request_uri; }
}

server {
  listen 443 ssl;
  listen [::]:443 ssl;
  http2 on;
  server_name stormvpn.example;

  ssl_certificate /etc/letsencrypt/live/stormvpn.example/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/stormvpn.example/privkey.pem;
  ssl_protocols TLSv1.2 TLSv1.3;
  ssl_prefer_server_ciphers off;
  ssl_session_cache shared:SSL:10m;
  ssl_session_timeout 1d;
  ssl_session_tickets off;
  ssl_stapling on;
  ssl_stapling_verify on;

  add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;
  include /etc/nginx/snippets/security-headers.conf;
  # include /etc/nginx/snippets/cloudflare-realip.conf;
  include /etc/nginx/snippets/locations.conf;
}
